// pub trait Observer {
//     fn initial_read(&mut self, file: &mut FileWidget<U>, area: impl
// Area);
//
//     fn read_change(&mut self, file: &mut FileWidget<U>, change:
// &Change, area: impl Area); }
